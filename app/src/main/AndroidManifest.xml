<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.wassersportclub.wcg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TeilnehmerÄnderungsTabelle"></activity>
        <activity android:name=".TeilnehmerVerwaltung" />
        <activity android:name=".TeilnehmerErstellTabelle" />
        <activity android:name=".BlauesbandRegeln" />
        <activity android:name=".Regatta" />
        <activity android:name=".RegattaAuswahl" />
        <activity android:name=".Blauesband" />
        <activity android:name=".Steganlage" />
        <activity android:name=".Startseite" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>